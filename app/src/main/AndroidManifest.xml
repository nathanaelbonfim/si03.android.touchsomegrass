<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.touchsomegrass">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TouchSomeGrass">
        <activity
            android:name=".About"
            android:exported="false"
            android:label="@string/activity_about_title"/>
        <activity
            android:name=".Timer"
            android:exported="false"
            android:label="@string/activity_timer_title" />
        <activity
            android:name=".Configuration"
            android:exported="false"
            android:label="@string/activity_configuration_title" />
        <activity
            android:name=".Presentation"
            android:exported="true"
            android:label="@string/activity_presentation_title">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>